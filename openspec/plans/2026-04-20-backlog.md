# Backlog — 待辦事項（2026-04-20 本輪結束後的待處理）

> 本檔記錄今天這一輪會話結束時尚未完成的事項，依優先級排序。
> 實作前請先對齊 [knowledge-layer spec](../specs/knowledge-layer/spec.md) 與 [extraction plan](./2026-04-20-knowledge-layer-extraction.md)。

---

## 🔴 P0 — 與 comment-feedback 品質直接相關

### 1. 附錄截圖：各分頁完整抓取 ✅ 已完成 2026-04-23
- **現況：** engine 附錄寫死用 `screenshots/00-initial.png` + `01-social-overview.png`，兩張內容幾乎相同（封面 + 社群總覽），無意義
- **要做：** 改 `gslides.js` appendix 段，改從 `narrative.appendix_screenshots` 讀檔名列表；同時補拍 5 張各分頁完整截圖（社群總覽 / 趨勢 / 平台 / 好感度 / KOL）
- **何處：** `core/engines/renderers/gslides.js` 的 appendix 段 + `runs/<id>/screenshots/`
- **留言來源：** [AAAB4sHtAxA] — user 已指出 P11/P12 重複
- **預估：** 1 小時（含重拍）

### 2. Google Slides Chart Embed
- **現況：** engine 只支援表格，user 多次留言要「視覺化圖表」
- **要做：** 實作 Google Slides API 的 `createSheetsChart` 或 `createImage`（寫 bar/pie chart 到 Google Sheets 再嵌入）
- **何處：** 新增 `core/engines/renderers/chart-helper.js` + 串入 `gslides.js` renderChartElement
- **留言來源：** [AAAB4sHtAws] / [AAAB4sHtAwo] 等多則
- **預估：** 4–8 小時（含 Sheets API 串接 + schema 擴充 `chapter.charts`）

---

## 🟡 P1 — 版面系統性改善（「一勞永逸」）

### 3. Auto-layout 依文字長度動態排版
- **現況：** engine 用絕對座標（x/y/w/h 固定），文字超長只能 workaround（調常數）
- **要做：** 改成量測文字高度後動態計算 y 偏移；或改用 Slides 的 placeholder layout system
- **何處：** `core/engines/renderers/gslides.js` 的 `buildChapterSlide` / `addHeader` / `addStyledText`
- **留言來源：** [AAAB4sHtAwk] 「有沒有更好的方法可以一勞永逸」
- **預估：** 8–16 小時（研究 Slides API layout primitive + 重構）

### 4. Narrative 章節 schema 擴充：`charts` / `appendix_screenshots` / `subtitle`
- 現有 schema 沒有 charts 欄位；appendix 沒定義；subtitle 有但 layout 不靈活
- 搭配 P0.1 和 P1.3 一起做

---

## 🟢 P2 — Knowledge Layer 完整化

### 5. Rerun / lineage / is_final CLI
- Spec 已在 [knowledge-layer/spec.md](../specs/knowledge-layer/spec.md) 定義
- 需實作：`pipeline rerun <run-id>` / `pipeline mark-final` / `pipeline lineage` / `pipeline run-diff`

### 6. Run Context manifest
- 使用者丟 PDF / 競品報告 / 網址當參考資料時的 `runs/<id>/context/manifest.yaml`
- Spec 已定義，實作未動

### 7. Outputs manifest（統一本地檔 + 雲端連結）
- 目前手動維護的 `outputs/manifest.json` 沒被 engine 自動產
- 要做：engine 產出時自動寫入統一 manifest

### 8. Logs 跨 run index
- `logs/index.ndjson` / `logs/versions.ndjson`
- 目前完全沒實作

---

## 🔵 P3 — 技術債 / SoC 深化

### 9. engine.js 產新檔時的 brand-colors 自動注入
- 現況：`brand.json` 必須手動內嵌 `colors` 物件才會被 engine 套用
- 改善：engine 依 `brand_color_key` 從 `templates/brand-colors.json` 自動查表注入
- 現在作法手動維護 brand.json 冗餘

### 10. engines/ UI 訊息中文字面量（baseline 407 條）
- 目前 SoC 守門機制只擋「新增違規」，既有違規做為 baseline
- 長期處理：建 i18n 層（`locales/zh-TW.json`）或繼續逐步搬到 knowledge 層
- 主要涉及：`comment-feedback/*` UI 訊息、`engine.js` PPT 渲染層、`validator.js` 警告

### 11. Bot 身份擴充：engine.js 內部操作改用 SA？
- 現況：engine.js 建新檔用 user OAuth，之後 auto-grant SA writer
- 可改為：engine.js 用 SA 建檔，SA 本身就有 Drive quota（需 Shared Drive 或 domain-wide delegation）
- 好處：完全脫離 user token，可做 24/7 scheduled task

### 12. Knowledge-loader 錯誤訊息中文化
- 現在所有錯誤都英文 `[knowledge-loader] missing key: ...`
- 符合 SoC（engines 不放中文）但使用者體驗差
- 方案：錯誤訊息走 i18n 層（同 P3.10）

---

## 📝 備註

- **優先級排序原則：** P0 = 使用者留言點名、P1 = 系統品質、P2 = 未實作 spec、P3 = 技術債
- **今日已完成事項彙整於 [CHANGELOG.md](../../CHANGELOG.md)**
- **本次 run outputs：** `~/.fontrends/runs/taipei-101-2026-04-20/` — Google Slides `1olMKIQgeIcqBacGzcTbYEy4_OD8CcHIRtH2CMX5ED4c`
